Comparing Thing
Conditional statementstransform the script from being just a list of instructions to somehthing that can make decisions on the fly based on the informaiton that it receives. 
comparison operators:

<:Less than
<=:Less Than or equal
>:Greater than
>=Greater than or equal
!=:Not equal

Operands are the bits of data on the left and right of the operator in an expression. 
The operands on either side of the comparison operator must be of types that can be compared. 
*The operands and the comparison operator together are a conditional expression.*

Combining conditional expression can be combined to produce relatively complex conditions using the logical operators, and or.

Using Dictionaries
Dictionaries are used anywhere want to store arributes andvalues that describe some concpet or entity.

Sorting dictionaries
Sort the output from any of the dictionary methods using the built in sorted () function.

Set
Using the if item in set statement. Notice that creating a set automatically removes any duplicate entires. 

Stacks and Queues
A stack is a last in, first out structure, used rather like the discard pile in a card game. 
Can push items ono the stack with list.append() and pop them back off with pop().

Multidimensional list
Lists can be multidimensional, that is to say, nested insdie other lists. List containing other lists can be used as a way of storing a matrix, or table of information. 

